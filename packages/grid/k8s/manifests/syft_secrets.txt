apiVersion: v1
kind: Secret
metadata:
  name: "syftsecret"
type: Opaque
data:
  {{- if not .Values.configuration.devmode }}

  {{- $secretObj := (lookup "v1" "Secret" .Release.Namespace "syftsecret") | default dict }}
  {{- $secretData := (get $secretObj "data") | default dict }}

  {{- $stackApiKey := (get $secretData "mongo-username-secret") | default (randAlphaNum 32 | b64enc) }}
  stackApiKey: {{ $stackApiKey | quote }}

  {{- $defaultRootPassword := (get $secretData "mongo-password-secret") | default (randAlphaNum 32 | b64enc) }}
  defaultRootPassword: {{ $defaultRootPassword | quote }}
  
  {{- else }}
  stackApiKey: Y2hhbmdlbWU= # Base64-encoded "changeme"
  defaultRootPassword: Y2hhbmdldGhpcw== # Base64-encoded "changethis"
  {{- end }}
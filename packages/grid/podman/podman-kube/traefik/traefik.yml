global:
  checkNewVersion: false
  sendAnonymousUsage: false

log:
  level: INFO

entryPoints:
  web:
    address: :81

ping:
  manualRouting: true

providers:
  file:
    directory: /traefik_config/conf
    watch: true

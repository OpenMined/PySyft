#!/usr/bin/env python
# coding: utf-8

# ## sklearn.metrics._scorer._PredictScorer

# In[ ]:


# third party
import sklearn


def class_constructor(*args, **kwargs):
    obj = sklearn.metrics._scorer._PredictScorer()
    return obj


# In[ ]:


# sklearn.metrics._scorer._PredictScorer.__call__
try:
    obj = class_constructor()  # noqa F821
    ret = obj.__call__()
    type_sklearn_metrics__scorer__PredictScorer___call__ = (
        getattr(ret, "__module__", "none") + "." + ret.__class__.__name__
    )
    print("✅ sklearn.metrics._scorer._PredictScorer.__call__: ", type(ret))  # noqa E501
except Exception as e:
    type_sklearn_metrics__scorer__PredictScorer___call__ = "_syft_missing"
    print(
        "❌ sklearn.metrics._scorer._PredictScorer.__call__: Return unavailable"
    )  # noqa E501
    print("  Please fix this return type code until there is no exception")
    print("  Error:", e)


# In[ ]:


# sklearn.metrics._scorer._PredictScorer.__repr__
try:
    obj = class_constructor()  # noqa F821
    ret = obj.__repr__()
    type_sklearn_metrics__scorer__PredictScorer___repr__ = (
        getattr(ret, "__module__", "none") + "." + ret.__class__.__name__
    )
    print("✅ sklearn.metrics._scorer._PredictScorer.__repr__: ", type(ret))  # noqa E501
except Exception as e:
    type_sklearn_metrics__scorer__PredictScorer___repr__ = "_syft_missing"
    print(
        "❌ sklearn.metrics._scorer._PredictScorer.__repr__: Return unavailable"
    )  # noqa E501
    print("  Please fix this return type code until there is no exception")
    print("  Error:", e)


# In[ ]:


# sklearn.metrics._scorer._PredictScorer._check_pos_label
try:
    obj = class_constructor()  # noqa F821
    ret = obj._check_pos_label()
    type_sklearn_metrics__scorer__PredictScorer__check_pos_label = (
        getattr(ret, "__module__", "none") + "." + ret.__class__.__name__
    )
    print(
        "✅ sklearn.metrics._scorer._PredictScorer._check_pos_label: ", type(ret)
    )  # noqa E501
except Exception as e:
    type_sklearn_metrics__scorer__PredictScorer__check_pos_label = "_syft_missing"
    print(
        "❌ sklearn.metrics._scorer._PredictScorer._check_pos_label: Return unavailable"
    )  # noqa E501
    print("  Please fix this return type code until there is no exception")
    print("  Error:", e)


# In[ ]:


# sklearn.metrics._scorer._PredictScorer._factory_args
try:
    obj = class_constructor()  # noqa F821
    ret = obj._factory_args()
    type_sklearn_metrics__scorer__PredictScorer__factory_args = (
        getattr(ret, "__module__", "none") + "." + ret.__class__.__name__
    )
    print(
        "✅ sklearn.metrics._scorer._PredictScorer._factory_args: ", type(ret)
    )  # noqa E501
except Exception as e:
    type_sklearn_metrics__scorer__PredictScorer__factory_args = "_syft_missing"
    print(
        "❌ sklearn.metrics._scorer._PredictScorer._factory_args: Return unavailable"
    )  # noqa E501
    print("  Please fix this return type code until there is no exception")
    print("  Error:", e)


# In[ ]:


# sklearn.metrics._scorer._PredictScorer._score
try:
    obj = class_constructor()  # noqa F821
    ret = obj._score()
    type_sklearn_metrics__scorer__PredictScorer__score = (
        getattr(ret, "__module__", "none") + "." + ret.__class__.__name__
    )
    print("✅ sklearn.metrics._scorer._PredictScorer._score: ", type(ret))  # noqa E501
except Exception as e:
    type_sklearn_metrics__scorer__PredictScorer__score = "_syft_missing"
    print(
        "❌ sklearn.metrics._scorer._PredictScorer._score: Return unavailable"
    )  # noqa E501
    print("  Please fix this return type code until there is no exception")
    print("  Error:", e)


# In[ ]:


# sklearn.metrics._scorer._PredictScorer._select_proba_binary
try:
    obj = class_constructor()  # noqa F821
    ret = obj._select_proba_binary()
    type_sklearn_metrics__scorer__PredictScorer__select_proba_binary = (
        getattr(ret, "__module__", "none") + "." + ret.__class__.__name__
    )
    print(
        "✅ sklearn.metrics._scorer._PredictScorer._select_proba_binary: ", type(ret)
    )  # noqa E501
except Exception as e:
    type_sklearn_metrics__scorer__PredictScorer__select_proba_binary = "_syft_missing"
    print(
        "❌ sklearn.metrics._scorer._PredictScorer._select_proba_binary: Return unavailable"
    )  # noqa E501
    print("  Please fix this return type code until there is no exception")
    print("  Error:", e)

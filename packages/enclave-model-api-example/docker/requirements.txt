# Model deps for the inference image only — kept out of this package's deps
# (and the root uv.lock) so test environments stay jax-free.
# `gemma` (DeepMind, JAX) pulls jax, flax, orbax and the tokenizer deps.
gemma>=3.0.0

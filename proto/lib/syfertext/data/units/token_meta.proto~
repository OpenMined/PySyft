syntax = "proto3";

package syft.lib.syfertext;

import "proto/lib/attribute_values.proto"

message TokenMeta{
    string text = 1;
    bool space_after = 2;
    map<string, AttributeValues> attributes = 3
}
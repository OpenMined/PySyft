apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pysyft.fullname" . }}-test-connection"
  labels:
    {{- include "pysyft.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pysyft.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

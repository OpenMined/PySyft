apiVersion: v1
kind: Service
metadata:
  name: {{ include "pysyft.fullname" . }}
  labels:
    {{- include "pysyft.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pysyft.selectorLabels" . | nindent 4 }}
